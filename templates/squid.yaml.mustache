manifestVersion: subsquid.io/v0.1
name: {{projectName}}
description: {{projectDescription}}
build: 
deploy:
  addons:
    postgres: 
    rpc:
{{#networks}}
{{#shortName}}
      - {{shortName}}.http
{{/shortName}}
{{/networks}}
  processor:
{{#networks}}
    - name: {{name}}-processor
      cmd: [ "sqd", "process:{{name}}:prod" ]
{{/networks}}
  api:
    cmd:
      - sqd
      - serve:prod
#scale:
#  addons:
#    postgres:
#      storage: 200G
